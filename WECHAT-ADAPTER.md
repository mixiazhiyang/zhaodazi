# 小红书找搭子 - 微信/飞书对话适配

本文档定义通过微信、飞书等即时通讯工具使用小红书找搭子功能的交互协议。

## 设计原则

1. **移动优先** - 所有操作通过自然语言对话完成，无需编辑配置文件
2. **链接可点** - 所有小红书链接可直接点击跳转
3. **信息精简** - 消息内容简洁，适合手机屏幕阅读
4. **即时反馈** - 每个操作都有明确的结果反馈

## 功能列表

| 功能 | 用户触发词 | 后台操作 |
|------|------------|----------|
| 找搭子 | `找搭子 爬山 北京`<br>`帮我找探店搭子`<br>`小红书找打球的人` | 1. 解析活动类型和城市<br>2. 搜索相关帖子<br>3. 质量筛选（过滤营销帖）<br>4. 作者活跃度分析<br>5. 智能生成个性化评论<br>6. 自动评论并记录<br>7. 返回评论结果（带小红书链接） |
| 查看回复 | `查看回复`<br>`有人回复我吗`<br>`检查消息` | 1. 读取已评论帖子列表<br>2. 逐个检查是否有新回复<br>3. 识别作者回复<br>4. 返回回复列表（带小红书链接和回复内容） |
| 查看配置 | `我的配置`<br>`查看设置`<br>`个人信息` | 读取 config.json，返回当前用户 profile、搜索默认值、筛选条件 |
| 修改配置 | `修改城市 上海`<br>`设置兴趣 摄影,徒步`<br>`修改昵称 小李`<br>`修改简介 喜欢户外运动` | 更新 config.json 对应字段，返回修改结果 |
| 查看历史 | `评论历史`<br>`我评论了哪些`<br>`最近的记录` | 读取 commented-posts.csv，返回最近 N 条评论记录（带链接） |
| 帮助 | `帮助`<br>`怎么用`<br>`功能列表` | 返回所有可用命令和示例 |

## 消息格式规范

### 1. 找搭子 - 执行结果

**格式**：
```
✅ 已评论 3 个帖子

━━━━━━━━━━━━━━━━━━
📝 周末爬山求组队
👤 小明
💬 我也在北京！周末正好有空
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221a7

━━━━━━━━━━━━━━━━━━
📝 香山徒步找搭子
👤 小红
💬 同城！一起吗？私信聊聊
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221b8

━━━━━━━━━━━━━━━━━━
📝 周末户外活动组队
👤 小李
💬 好巧我也想去，私聊！
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221c9

📊 统计：
• 搜索了 25 个帖子
• 质量筛选后剩余 8 个
• 作者分析后剩余 5 个
• 成功评论 3 个
• 跳过 2 个（已评论过）

💡 提示：留意小红书私信，作者可能会联系你
```

**字段说明**：
- 📝 帖子标题
- 👤 作者昵称
- 💬 你的评论内容
- 🔗 小红书帖子链接（可点击）

### 2. 查看回复 - 有新回复

**格式**：
```
🎉 有 2 条新回复！

━━━━━━━━━━━━━━━━━━
📝 周末爬山求组队
👤 小明
💬 你的评论：我也在北京！周末正好有空
💭 作者回复：好啊！私信你了，周六早上8点集合
⏰ 2026-05-04 14:30
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221a7

━━━━━━━━━━━━━━━━━━
📝 香山徒步找搭子
👤 小红
💬 你的评论：同城！一起吗？私信聊聊
💭 作者回复：已私信你啦
⏰ 2026-05-04 15:20
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221b8

💡 提示：快去小红书查看私信吧！
```

### 3. 查看回复 - 无新回复

**格式**：
```
📭 暂时没有新回复

已检查 5 个帖子，都还没有作者回复。

💡 提示：可以稍后再试，或者继续找更多搭子
```

### 4. 查看配置

**格式**：
```
👤 个人信息
━━━━━━━━━━━━━━━━━━
昵称：小明
年龄：25
城市：北京
兴趣：爬山, 探店, 摄影
简介：周末喜欢户外活动，想认识更多朋友

⚙️ 搜索设置
━━━━━━━━━━━━━━━━━━
时间范围：最近 3 天
单次最多评论：20 条
评论间隔：3-5 秒

🔍 筛选条件
━━━━━━━━━━━━━━━━━━
最少评论数：0
最多评论数：10
黑名单词：（无）

💡 修改配置：发送"修改城市 上海"或"设置兴趣 摄影,徒步"
```

### 5. 修改配置 - 成功

**格式**：
```
✅ 配置已更新

城市：北京 → 上海

💡 下次找搭子时会优先匹配上海的帖子
```

### 6. 修改配置 - 失败

**格式**：
```
❌ 配置更新失败

不支持的配置项：年龄上限

💡 可修改的配置：
• 修改城市 [城市名]
• 修改昵称 [昵称]
• 设置兴趣 [兴趣1,兴趣2,...]
• 修改简介 [个人简介]
```

### 7. 查看历史

**格式**：
```
📝 最近 5 条评论记录

━━━━━━━━━━━━━━━━━━
⏰ 2026-05-04 14:30
📝 周末爬山求组队
👤 小明
💬 我也在北京！周末正好有空
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221a7
✅ 已回复

━━━━━━━━━━━━━━━━━━
⏰ 2026-05-04 13:15
📝 香山徒步找搭子
👤 小红
💬 同城！一起吗？私信聊聊
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221b8
⏳ 等待回复

━━━━━━━━━━━━━━━━━━
⏰ 2026-05-03 16:45
📝 周末户外活动组队
👤 小李
💬 好巧我也想去，私聊！
🔗 https://www.xiaohongshu.com/explore/69f5680500000000350221c9
⏳ 等待回复

...

💡 查看更多：发送"查看历史 10"
```

### 8. 帮助

**格式**：
```
🤖 小红书找搭子 Bot

━━━━━━━━━━━━━━━━━━
📌 主要功能

1️⃣ 找搭子
发送：找搭子 [活动] [城市]
示例：找搭子 爬山 北京

2️⃣ 查看回复
发送：查看回复
查看是否有作者回复你的评论

3️⃣ 查看配置
发送：我的配置
查看当前个人信息和设置

4️⃣ 修改配置
发送：修改城市 [城市名]
发送：设置兴趣 [兴趣1,兴趣2]
发送：修改昵称 [昵称]
发送：修改简介 [个人简介]

5️⃣ 查看历史
发送：评论历史
查看最近的评论记录

━━━━━━━━━━━━━━━━━━
💡 提示：
• 所有链接可直接点击跳转小红书
• 评论后记得查看小红书私信
• 建议每天运行一次"查看回复"
```

## 首次使用引导

用户第一次使用时，如果 `config.json` 不存在，Bot 应该引导用户完成配置：

**消息流程**：

```
👋 欢迎使用小红书找搭子 Bot！

首次使用需要简单配置一下，帮助我更好地为你找到合适的搭子。

━━━━━━━━━━━━━━━━━━
请回答以下问题（可随时修改）：

1️⃣ 你的昵称是？
（用于生成自然的评论）
```

用户回复后：
```
✅ 昵称：小明

2️⃣ 你在哪个城市？
（会优先匹配同城帖子）
```

用户回复后：
```
✅ 城市：北京

3️⃣ 你的兴趣爱好？
（用逗号分隔，如：爬山,探店,摄影）
```

用户回复后：
```
✅ 兴趣：爬山, 探店, 摄影

4️⃣ 简单介绍下自己？
（用于生成个性化评论）
```

用户回复后：
```
✅ 配置完成！

👤 你的信息
━━━━━━━━━━━━━━━━━━
昵称：小明
城市：北京
兴趣：爬山, 探店, 摄影
简介：周末喜欢户外活动，想认识更多朋友

💡 现在可以开始找搭子了！
发送：找搭子 爬山 北京
```

## 错误处理

### 1. 未登录小红书

**格式**：
```
❌ 小红书账号未登录

请先登录小红书账号才能使用找搭子功能。

💡 登录方式：
1. 在电脑上打开 Chrome 浏览器
2. 登录 xiaohongshu.com
3. 发送"重新登录"让我提取 Cookie

或者联系管理员配置二维码登录。
```

### 2. Cookie 过期

**格式**：
```
⚠️ 登录已过期

小红书 Cookie 已失效（有效期 7 天）。

💡 请重新登录：
发送"重新登录"
```

### 3. 搜索无结果

**格式**：
```
😔 没有找到相关帖子

搜索词：爬山搭子 北京
时间范围：最近 3 天

💡 建议：
• 尝试更宽泛的搜索词（如"户外活动"）
• 扩大时间范围（发送"修改时间范围 7"）
• 换个城市试试
```

### 4. 评论失败

**格式**：
```
❌ 评论失败

帖子：周末爬山求组队
原因：触发风控，请稍后再试

💡 建议：
• 等待 10 分钟后重试
• 减少单次评论数量
• 检查账号是否正常
```

## 技术实现要点

### 1. 命令解析

Agent 需要从自然语言中提取：
- **找搭子**: 活动类型、城市（可选）
- **修改配置**: 配置项、新值
- **查看历史**: 数量（可选，默认 5）

### 2. 链接构造

小红书帖子链接格式：
```
https://www.xiaohongshu.com/explore/{note_id}
```

从搜索结果的 `id` 字段直接构造。

### 3. 消息长度控制

- 单条消息不超过 2000 字符（微信限制）
- 如果评论结果过多，分批发送
- 历史记录默认显示 5 条，可指定数量

### 4. 异步通知

回复监控应该支持主动推送：
- 定时任务（如每小时检查一次）
- 发现新回复时主动发送消息给用户
- 避免用户频繁手动查询

### 5. 多用户支持

如果 Bot 服务多个用户：
- 每个用户独立的 `config.json`（如 `config-{user_id}.json`）
- 每个用户独立的 `commented-posts-{user_id}.csv`
- 根据微信/飞书用户 ID 区分

## 集成示例

### 微信 Bot (基于 wechaty)

```javascript
// 伪代码示例
bot.on('message', async (msg) => {
  const text = msg.text();
  const userId = msg.from().id;
  
  if (text.startsWith('找搭子')) {
    // 调用 xhs-find skill
    const result = await runSkill('xhs-find', {
      userId,
      query: text,
      channel: 'wechat'
    });
    
    // 格式化为微信消息
    const formatted = formatForWechat(result);
    await msg.say(formatted);
  }
  
  if (text === '查看回复') {
    // 调用回复监控
    const replies = await checkReplies(userId);
    const formatted = formatRepliesForWechat(replies);
    await msg.say(formatted);
  }
});
```

### 飞书 Bot (基于 Lark SDK)

```javascript
// 伪代码示例
lark.on('message', async (event) => {
  const text = event.message.content;
  const userId = event.sender.sender_id.user_id;
  
  if (text.startsWith('找搭子')) {
    const result = await runSkill('xhs-find', {
      userId,
      query: text,
      channel: 'lark'
    });
    
    // 飞书支持富文本卡片
    const card = buildLarkCard(result);
    await lark.sendCard(event.message.chat_id, card);
  }
});
```

## 未来扩展

1. **语音输入** - 支持语音消息转文字后处理
2. **图片识别** - 用户发送小红书截图，自动识别帖子 ID
3. **定时任务** - "每天早上 9 点帮我找爬山搭子"
4. **智能推荐** - 根据历史记录推荐相似活动
5. **群聊模式** - 在微信群里使用，@Bot 触发
